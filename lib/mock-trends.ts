import { HourlyTrend } from "@/types/parking";

/**
 * Placeholder — replace me with real hourly trend data from Supabase when available.
 * The HourlyTrendChart component no longer calls this; it is kept to avoid breaking imports.
 */
export function getMockTrends(_lotId: string): HourlyTrend[] {
  return [];
}
