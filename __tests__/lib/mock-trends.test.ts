import { getMockTrends } from "@/lib/mock-trends";

describe("getMockTrends", () => {
  it("returns empty array (placeholder — replace me when real trend data is available)", () => {
    expect(getMockTrends("lot-w")).toHaveLength(0);
  });
});
